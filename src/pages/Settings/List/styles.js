const styles = theme => {
  return {
    menuItem: {
      '-webkit-app-region': 'no-drag',
      minWidth: 8,
      fontSize: 14,
    },
    menuScale: {
      transform: 'scale(.88)!important',
    },
    menuIcon: {
      '-webkit-app-region': 'no-drag',
      marginRight: 0,
    },
  };
};

export default styles;
